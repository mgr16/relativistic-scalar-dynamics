# Runtime dependencies (mirror of pyproject.toml [project.dependencies]).
# Dev tools (pytest, ruff, etc.) se instalan con: pip install -e .[dev]
numpy>=1.24
scipy>=1.10
mpi4py>=3.1
petsc4py>=3.19
gmsh>=4.11
PyYAML>=6.0
# DOLFINx is required and should be installed via conda:
#   conda install -c conda-forge fenics-dolfinx
